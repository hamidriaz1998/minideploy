# improve init-server.
    - if daemon is already uploaded, skip the upload step or add a --skip-bin-upload option
    - also if there is already a .deploy.yml file extract app-name from their and make the related dirs on server directly instead of passing the --app-name option
# improve deploy
    - add --skip-build option to skip the build step
    - add --skip-upload option to skip the upload step and use the upload already in `upload/` dir on the server

# Add verbose logging
    - add --verbose option to enable verbose logging
    - print individual steps
    - print commands ran
    - pretty print all of these
